import AttendanceEntry from "@/components/AttendanceEntry";
import { useState } from "react";

export default function Attentry(){

    const [student,setStudent] = useState<number>(1);
    return <div>
        
    </div>
}